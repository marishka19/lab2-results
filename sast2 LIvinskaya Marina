# Лабораторная работа 2: Clang static analyzer и CVE-2014-8130
## Студент: Марина Ливинская (Винокурова)

## Цель работы
Исправить уязвимость CVE-2014-8130 в библиотеке libtiff с помощью статического анализатора Clang.

## Результаты

### 1. Статический анализ ДО исправления
- Всего ошибок: 139
- Division by zero: 11
- Файл: `libtiff/tif_write.c:119`

### 2. Статический анализ ПОСЛЕ исправления
- Всего ошибок: 7
- Division by zero: 2 (уменьшилось с 11!)
- Исправление работает

### 3. Исправление уязвимости
- Добавлена проверка: `if (td->td_stripsperimage == 0)`
- Функция: `TIFFSetupStrips()` в файле `tif_write.c`
- Патч: [fix_cve_2014_8130.patch](fix_cve_2014_8130.patch)

### 4. Файлы для проверки
1. `fix_cve_2014_8130.patch` - патч с исправлением
2. `before_analysis.png` - отчет анализатора до исправления
3. `after_analysis.png` - отчет анализатора после исправления

## Вывод
Уязвимость CVE-2014-8130 (деление на ноль) успешно исправлена. Количество срабатываний статического анализатора уменьшилось с 11 до 2.
